version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd website
        - make install
    build:
      commands:
        - make build
  artifacts:
    baseDirectory: "/website/build/"
    files:
      - "**/*"
  cache:
    paths:
      - "node_modules/**/*"
