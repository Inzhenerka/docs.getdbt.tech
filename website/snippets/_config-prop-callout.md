<span>{props.title} — это «особые свойства», потому что вы не можете настраивать их в файле <code>dbt_project.yml</code> или с помощью блоков <code>config()</code>. Подробнее см. <a href="https://docs.getdbt.com/reference/define-properties#which-properties-are-not-also-configs" target="_self">Конфигурация и свойства</a>.</span>
