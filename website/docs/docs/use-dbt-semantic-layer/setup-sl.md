---
title: "Администрирование семантического слоя"
id: setup-sl
description: "Бесшовная настройка учетных данных и токенов для начала выполнения запросов к семантическому слою dbt."
sidebar_label: "Администрирование семантического слоя"
tags: [Semantic Layer]
pagination_next: "docs/use-dbt-semantic-layer/sl-architecture"
pagination_prev: "guides/sl-snowflake-qs"
---

# Администрирование Semantic Layer <Lifecycle status="self_service,managed,managed_plus" /> {#administer-the-semantic-layer}

С помощью dbt <Constant name="semantic_layer" /> вы можете централизованно определять бизнес-метрики, снижать дублирование кода и несогласованность, создавать возможности self-service в downstream-инструментах и многое другое. В этом разделе показано, как настроить учетные данные и токены, чтобы другие инструменты могли выполнять запросы к <Constant name="semantic_layer" />.

:::info Пока не поддерживается в движке dbt Fusion
<Constant name="semantic_layer" /> в настоящее время поддерживается в <Constant name="dbt_platform" /> для окружений, работающих на версиях <Constant name="core" />. Поддержка окружений на движке dbt Fusion появится в ближайшее время.
:::

## Предварительные условия {#prerequisites}

import SetUp from '/snippets/_v2-sl-prerequisites.md';

<SetUp/>

import SLCourses from '/snippets/_sl-course.md';

<SLCourses/>

## Администрирование семантического слоя {#administer-the-semantic-layer-1}

import SlSetUp from '/snippets/_new-sl-setup.md';  

<SlSetUp/>

<!--
1. Создайте новое окружение в <Constant name="cloud" />, выбрав **Deploy**, а затем **Environments**.
2. Выберите **dbt Version 1.6** (или более новую) и введите учетные данные для деплоя.
3. Чтобы настроить новый Semantic Layer, в новом окружении должен быть хотя бы один успешный запуск. Мы рекомендуем выполнить `dbt ls`, так как `dbt build` не выполнится успешно до тех пор, пока вы не создадите и не определите семантические модели и метрики.
4. Чтобы включить <Constant name="semantic_layer" />, перейдите на страницу **Account Settings**, затем выберите конкретный проект, для которого вы хотите включить <Constant name="semantic_layer" />.
5. На странице **Project Details** выберите **Configure <Constant name="semantic_layer" />.** Вам будет предложено ввести учетные данные подключения к платформе данных для <Constant name="semantic_layer" /> и выбрать окружение, в котором вы хотите его включить. Мы рекомендуем использовать учетные данные с минимально необходимыми правами. Semantic Layer требует разрешений SELECT и CREATE TABLE.
6. После ввода учетных данных вы увидите информацию о подключении, которую можно использовать для интеграции с downstream-инструментами. Если используемый инструмент поддерживает подключение через JDBC, вы можете сохранить **JDBC URL** или отдельные компоненты подключения (например, environment id, host). В качестве альтернативы, если инструмент использует GraphQL API <Constant name="semantic_layer" />, сохраните информацию о хосте GraphQL API.
7. Затем вернитесь на страницу **Project Details** и выберите **Generate Service Token**, чтобы создать сервисный токен <Constant name="semantic_layer" />. Сохраните этот токен — он понадобится позже.
8. Готово 🎉! Semantic Layer теперь включен для вашего проекта.
-->

## Следующие шаги {#next-steps}

- Теперь, когда вы настроили свои учетные данные и токены, начните выполнять запросы к метрикам с помощью [доступных интеграций](/docs/cloud-integrations/avail-sl-integrations).
- [Оптимизируйте производительность запросов](/docs/use-dbt-semantic-layer/sl-cache) с помощью декларативного кэширования.
- [Проверяйте семантические узлы в CI](/docs/deploy/ci-jobs#semantic-validations-in-ci), чтобы убедиться, что изменения кода в моделях dbt не нарушают работу этих метрик.
- Если вы еще этого не сделали, изучите, как [создавать метрики и семантические модели](/docs/build/build-metrics-intro) в выбранном вами инструменте разработки.
- Ознакомьтесь с часто задаваемыми вопросами о [<Constant name="semantic_layer" />](/docs/use-dbt-semantic-layer/sl-faqs).

## Часто задаваемые вопросы {#faqs}

<DetailsToggle alt_header="Как кэширование взаимодействует с контролем доступа?">

Кэшированные данные хранятся отдельно от базовых моделей. Если метрики извлекаются из кэша, мы не применяем контекст безопасности к этим таблицам во время выполнения запроса.

В будущем мы планируем клонировать учетные данные, определять минимальный необходимый уровень доступа и применять эти разрешения к кэшированным таблицам.

</DetailsToggle>