---
title: "Подключение через Git clone"
description: "Узнайте, как подключиться к git-репозиторию с помощью git URL."
---

В <Constant name="cloud" /> вы можете импортировать git‑репозиторий по любому корректному git URL, который указывает на dbt‑проект. При этом есть несколько важных моментов, которые нужно учитывать.

## Протоколы Git {#git-protocols}
Вы должны использовать версию git URL в формате `git@...` или `ssh://...`, а не `https://...`. <Constant name="cloud" /> использует SSH‑протокол для клонирования репозиториев, поэтому <Constant name="cloud" /> не сможет клонировать репозитории, указанные с использованием HTTP‑протокола.

### Преобразование SSH URL в корректный формат {#converting-ssh-urls-to-the-correct-format}

Когда вы копируете SSH URL из вашего git‑провайдера (обычно он находится в **Code** → **Clone** → **SSH**), он может быть в формате SCP, где для разделения хоста и пути используется двоеточие (`:`). Для <Constant name="cloud" /> такой URL необходимо преобразовать в стандартный формат SSH.

Если ваш SSH URL выглядит так (SCP‑стиль с двоеточием):

```
git@github.com:organization/repo-name.git
user@custom-host.example.com:organization/repo-name.git
```

Преобразуйте его в стандартный SSH URL (используется `ssh://` и `/`):
```
ssh://git@github.com/organization/repo-name.git
ssh://user@custom-host.example.com/organization/repo-name.git
```

import GitProvidersCI from '/snippets/_git-providers-supporting-ci.md';

<GitProvidersCI />

## Управление deploy keys {#managing-deploy-keys}

После импорта проекта по <Constant name="git" /> URL <Constant name="cloud" /> сгенерирует Deploy Key для вашего репозитория. Чтобы найти этот Deploy Key в <Constant name="cloud" />:

1. В <Constant name="cloud" /> нажмите на имя своей учетной записи в левом боковом меню и выберите **Account settings**.
2. Перейдите в раздел **Projects** и выберите нужный проект.
3. Нажмите на ссылку **Repository**, чтобы открыть страницу с деталями репозитория.
4. Скопируйте ключ в разделе **Deploy Key**.

Этот Deploy Key необходимо добавить в настройках репозитория у вашего <Constant name="git" />‑провайдера. Настройте его так, чтобы он имел *доступ на чтение и запись* к соответствующим репозиториям.

**На заметку**: Каждый проект в <Constant name="cloud" /> будет генерировать свой собственный deploy key при подключении к репозиторию, даже если два проекта подключены к одному и тому же репозиторию. Вам потребуется добавить оба deploy key у вашего <Constant name="git" />‑провайдера.

## GitHub {#github}

:::info Используете GitHub?

Если вы используете GitHub, вы можете импортировать свой репозиторий напрямую с помощью [GitHub Application от <Constant name="cloud" />](/docs/cloud/git/connect-github). Подключение репозитория через GitHub Application [включает Continuous Integration](/docs/deploy/continuous-integration).

:::

### Найдите ваш SSH URL {#locate-your-ssh-url}

1. Перейдите в свой репозиторий на GitHub.
2. Нажмите кнопку **Code**.
3. Выберите вкладку **SSH**, чтобы увидеть SSH URL для клонирования.
4. Скопируйте URL и при необходимости [преобразуйте его в корректный формат](#converting-ssh-urls-to-the-correct-format).

### Добавьте deploy key {#add-a-deploy-key}

Чтобы добавить deploy key в GitHub, перейдите на вкладку **Deploy keys** на странице настроек вашего репозитория GitHub.
- Укажите имя для deploy key и вставьте сам deploy key (сгенерированный <Constant name="cloud" />), затем обязательно установите флажок **Allow write access**.
- После добавления этого ключа <Constant name="cloud" /> сможет читать и записывать файлы в вашем dbt-проекте.
- Подробнее см. в документации GitHub: [Adding a deploy key in GitHub](https://github.blog/2015-06-16-read-only-deploy-keys/)

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/cd7351c-Screen_Shot_2019-10-16_at_1.09.41_PM.png" title="Настройка ключа развертывания GitHub"/>

## GitLab {#gitlab}

:::info Используете GitLab?

Если вы используете GitLab, вы можете импортировать свой репозиторий напрямую с помощью [GitLab Application от <Constant name="cloud" />](/docs/cloud/git/connect-gitlab). Подключение репозитория через GitLab Application [включает Continuous Integration](/docs/deploy/continuous-integration).

:::

- Чтобы добавить deploy key в аккаунт GitLab, перейдите на вкладку [SSH keys](https://gitlab.com/profile/keys) на странице User Settings вашего аккаунта GitLab.  
- Затем вставьте deploy key, сгенерированный <Constant name="cloud" /> для вашего репозитория.  
- После сохранения этого SSH-ключа <Constant name="cloud" /> сможет читать и записывать файлы в вашем GitLab-репозитории.  
- См. также: [Adding a read only deploy key in GitLab](https://docs.gitlab.com/ee/user/project/deploy_keys/)

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/f3ea88d-Screen_Shot_2019-10-16_at_4.45.50_PM.png" title="Настройка SSH-ключа GitLab"/>

## BitBucket {#bitbucket}

Используйте deploy key, чтобы импортировать ваш репозиторий BitBucket в <Constant name="cloud" />. Чтобы сохранить безопасность учетной записи, используйте сервисную учетную запись для добавления deploy key BitBucket и поддержания соединения между вашим репозиторием BitBucket и <Constant name="cloud" />.

BitBucket связывает каждый коммит в репозитории и другие действия <Constant name="git" /> (например, открытие pull request) с адресом электронной почты, связанным с учетной записью пользователя Bitbucket.

Чтобы добавить ключ развертывания в учетную запись BitBucket:

- Перейдите на вкладку **SSH keys** на странице **Personal Settings** вашего аккаунта BitBucket.
- Затем нажмите кнопку **Add key** и вставьте deploy key, сгенерированный <Constant name="cloud" /> для вашего репозитория.
- После сохранения этого SSH-ключа <Constant name="cloud" /> сможет читать и записывать файлы в вашем репозитории BitBucket.

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/bitbucket-ssh-key.png" title="Настройка SSH-ключа BitBucket"/>

## AWS CodeCommit {#aws-codecommit}

<Constant name="cloud" /> может работать с dbt‑проектами, размещёнными в AWS CodeCommit, однако по сравнению с GitHub или другими git‑провайдерами требуется выполнить несколько дополнительных шагов. В этом руководстве описано, как подключить dbt‑проект, размещённый в CodeCommit, к <Constant name="cloud" />.

:::warning AWS прекращает поддержку git‑хостинга CodeCommit

AWS CodeCommit больше недоступен для новых клиентов, а существующие клиенты получают только обновления безопасности и производительности. Подробнее см. в статье о [миграции на другого git‑провайдера](https://aws.amazon.com/blogs/devops/how-to-migrate-your-aws-codecommit-repository-to-another-git-provider/).

:::

#### Шаг 1: Создайте пользователя AWS для dbt {#step-1-create-an-aws-user-for-dbt}
- Чтобы предоставить <Constant name="cloud" /> доступ к вашему репозиторию, сначала необходимо создать пользователя AWS IAM для <Constant name="cloud" />.  
- Войдите в AWS Console и перейдите в раздел IAM.  
- Нажмите **Add User** и создайте нового пользователя, указав уникальное и понятное имя.  
- Пользователю потребуется доступ на клонирование репозитория. Это можно обеспечить, добавив разрешение **AWSCodeCommitPowerUser** во время настройки.

#### Шаг 2: Импортируйте репозиторий по имени {#step-2-import-your-repository-by-name}
- Откройте консоль AWS CodeCommit и выберите ваш репозиторий.  
- Скопируйте SSH URL на этой странице.  
- Затем перейдите на страницу **New Repository** в <Constant name="cloud" />.  
- Выберите вкладку **<Constant name="git" /> Clone** и вставьте SSH URL, скопированный из консоли.  
- На странице с деталями только что созданного репозитория вы увидите поле **Deploy Key**.  
- Скопируйте содержимое этого поля — оно понадобится вам на [Шаге 3](#step-3-grant-dbt-cloud-aws-user-access).

**Примечание:** публичный ключ, сгенерированный <Constant name="cloud" />, — единственный ключ, который будет работать на следующем шаге. Любой другой ключ, созданный вне <Constant name="cloud" />, работать не будет.

#### Шаг 3: Предоставьте пользователю AWS для dbt доступ {#step-3-grant-dbt-aws-user-access}
- Откройте только что созданного пользователя <Constant name="cloud" /> в консоли AWS IAM.  
- Перейдите на вкладку **Security Credentials** и нажмите **Upload SSH public key**.  
- Вставьте содержимое поля **Public Key** со страницы репозитория в <Constant name="cloud" />.  
- После создания ключа вы увидите для него значение **SSH key ID**.  
- **[Свяжитесь со службой поддержки dbt](mailto:support@getdbt.com)** и передайте им это значение, чтобы команда поддержки dbt могла завершить процесс настройки.

#### Шаг 4: Укажите пользовательскую ветку в dbt {#step-4-specify-a-custom-branch-in-dbt}

CodeCommit использует `master` в качестве ветки по умолчанию. Для инициализации проекта вам необходимо указать ветку `master` как [custom branch](/faqs/Environments/custom-branch-settings#development) в <Constant name="cloud" />.

- Перейдите в **Deploy** -> **Environments** -> **Development**.
- Выберите **Settings** -> **Edit** и в разделе **General Settings** установите флажок **Default to a custom branch**.
- Укажите пользовательскую ветку как `master` и нажмите **Save**.

#### Шаг 5: Настройте URL-адреса шаблона pull request (необязательно) {#step-5-configure-pull-request-template-urls-optional}

Чтобы предотвратить прямое слияние изменений кода в ветку по умолчанию, настройте [PR Template URL](/docs/cloud/git/pr-template) на странице **Repository details** вашего проекта. После настройки <Constant name="cloud" /> будет предлагать пользователям открыть новый PR после коммита и синхронизации изменений кода в ветке в <Constant name="cloud_ide" />, прежде чем сливать какие‑либо изменения в ветку по умолчанию.

- Перейдите в **Account Settings** -> **Projects** -> выберите проект.
- Нажмите на ссылку репозитория в разделе **Repository**.
- На странице **Repository details** нажмите **Edit** в правом нижнем углу.
  <Lightbox src="/img/docs/collaborate/repo-details.jpg" width="80%" title="Настройка шаблона PR на странице «Repository details»." />
- В поле **Pull request URL** укажите URL на основе рекомендуемого [формата PR template](/docs/cloud/git/pr-template#aws-codecommit).
- Замените `<repo>` на имя вашего репозитория (обратите внимание, что регистр символов имеет значение). В следующем примере имя репозитория — `New_Repo`.
  <Lightbox src="/img/docs/collaborate/pr-template-example.jpg" width="60%" title="В примере поля Pull request URL имя репозитория — «New_Repo»." />
- После заполнения поля **Pull request URL** нажмите **Save**.

🎉 **Готово!** После того как служба поддержки dbt обработает ваш запрос и вы настроите пользовательскую ветку, ваш проект будет готов к выполнению dbt‑запусков в <Constant name="cloud" />.

## Azure DevOps {#azure-devops}
:::info Используете Azure DevOps?

Если вы используете Azure DevOps и у вас тариф <Constant name="cloud" /> Enterprise или Enterprise+, вы можете импортировать ваш репозиторий напрямую с помощью [интеграции <Constant name="cloud" /> с Azure DevOps](/docs/cloud/git/connect-azure-devops). Подключение репозитория через приложение Azure DevOps [включает Continuous Integration](/docs/deploy/continuous-integration).

:::

1. Чтобы добавить ключ развертывания в учетную запись Azure DevOps, перейдите на страницу **SSH public keys** в настройках пользователя вашей учетной записи Azure DevOps или учетной записи служебного пользователя.

2. Мы рекомендуем использовать выделенную сервисную учётную запись для интеграции, чтобы подключение <Constant name="cloud" /> к Azure DevOps не прерывалось из‑за изменений прав доступа у пользователей.

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/52bfdaa-Screen_Shot_2020-03-09_at_4.13.20_PM.png" title="Перейдите на страницу настроек 'SSH public keys'" />

3. Затем нажмите кнопку **+ New Key**, чтобы создать новый SSH-ключ для репозитория.

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/6d8e980-Screen_Shot_2020-03-09_at_4.13.27_PM.png" title="Нажмите кнопку '+ New Key', чтобы создать новый SSH-ключ для репозитория." />

4. Выберите понятное и описательное имя для ключа, затем вставьте deploy key, сгенерированный <Constant name="cloud" /> для вашего репозитория.

5. После сохранения этого SSH-ключа <Constant name="cloud" /> сможет читать и записывать файлы в ваш репозиторий Azure DevOps.

<Lightbox src="/img/docs/dbt-cloud/cloud-configuring-dbt-cloud/d19f199-Screen_Shot_2020-03-09_at_4.13.50_PM.png" title="Введите и сохраните публичный ключ, сгенерированный dbt для вашего репозитория" />

## Другие git-провайдеры {#other-git-providers}

Не видите здесь своего git‑провайдера? Пожалуйста, [свяжитесь со службой поддержки dbt](mailto:support@getdbt.com) — мы с радостью поможем вам настроить <Constant name="cloud" /> с любым поддерживаемым git‑провайдером.

## Ограниченная интеграция {#limited-integration}
Некоторые возможности <Constant name="cloud" /> требуют тесной интеграции с вашим git‑хостингом, например обновление pull request’ов в GitHub статусами запусков <Constant name="cloud" />. Импорт проекта по URL не позволяет использовать эти функции. После того как вы предоставите <Constant name="cloud" /> доступ к вашему репозиторию, вы сможете продолжить настройку проекта, добавив подключение, а затем создав и запустив свою первую задачу <Constant name="cloud" />.

## Часто задаваемые вопросы {#faqs}
<FAQ path="Git/gitignore"/>
<FAQ path="Git/git-migration"/>
