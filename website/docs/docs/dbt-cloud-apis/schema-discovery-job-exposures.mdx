---
title: "Схема объекта Exposures"
sidebar_label: "Exposures"
id: "discovery-schema-job-exposures"
---

import { NodeArgsTable, SchemaTable } from "./schema";

Объект exposures позволяет запрашивать информацию обо всех exposures в рамках конкретного job. Подробнее см. в разделе [Add Exposures to your DAG](/docs/build/exposures).

### Аргументы {#arguments}

При выполнении запроса `exposures` доступны следующие аргументы.

<NodeArgsTable parent="JobNode" name="exposures" />

Ниже приведены иллюстративные примеры запросов и описание схемы объекта exposures.

### Пример запроса {#example-query}

В приведённом ниже примере запрашивается информация обо всех exposures в рамках конкретного job, включая имя и email владельца, URL, а также сведения о родительских sources и родительских models для каждого exposure.

```graphql
{
  job(id: 123) {
    exposures(jobId: 123) {
      runId
      projectId
      name
      uniqueId
      resourceType
      ownerName
      url
      ownerEmail
      parentsSources {
        uniqueId
        sourceName
        name
        state
        maxLoadedAt
        criteria {
          warnAfter {
            period
            count
          }
          errorAfter {
            period
            count
          }
        }
        maxLoadedAtTimeAgoInS
      }
      parentsModels {
        uniqueId
      }
    }
  }
}
```

### Поля {#fields}

При выполнении запроса `exposures` доступны следующие поля:

<SchemaTable nodeName="ExposureNode" />
