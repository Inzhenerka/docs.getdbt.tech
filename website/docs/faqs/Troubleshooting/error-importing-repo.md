---
title: Ошибки при импорте репозитория при настройке проекта dbt
description: "Ошибки при импорте репозитория при настройке проекта dbt"
sidebar_label: 'Ошибки при импорте репозитория при настройке проекта dbt'
id: error-importing-repo
---

Если вы не видите свой репозиторий в списке, проверьте следующее:
- Ваш репозиторий находится в группе GitLab, к которой у вас есть доступ. <Constant name="cloud" /> не читает репозитории, связанные с отдельным пользователем.

Если вы видите свой репозиторий в списке, но не можете успешно его импортировать, дважды проверьте, что:
- Вы являетесь мейнтейнером этого репозитория. Только пользователи с правами мейнтейнера могут настраивать подключения к репозиторию.

Если вы импортировали репозиторий с помощью нативной интеграции <Constant name="cloud" /> с GitLab, вы сможете увидеть, что в стратегии клонирования используется `deploy_token`. Если же используется SSH-ключ, это означает, что репозиторий был настроен не через нативную интеграцию с GitLab, а с помощью общего варианта клонирования git. В этом случае репозиторий необходимо переподключить, чтобы получить преимущества, описанные выше.
