---
title: "Быстрый старт с dbt Mesh"
id: "mesh-qs"
level: 'Intermediate'
icon: 'guides'
tags: ['dbt platform','Quickstart']
hide_table_of_contents: true
---

<div style={{maxWidth: '900px'}}>

## Введение

<Constant name="mesh" /> — это фреймворк, который помогает организациям эффективно масштабировать команды и дата‑активы. Он продвигает лучшие практики управления (governance) и позволяет разбивать крупные проекты на управляемые части, обеспечивая более быструю разработку аналитики. <Constant name="mesh" /> доступен для аккаунтов [<Constant name="cloud" /> Enterprise](https://www.getdbt.com/).

В этом руководстве вы узнаете, как настроить мультипроектную архитектуру, используя базовые концепции [<Constant name="mesh" />](https://www.getdbt.com/blog/what-is-data-mesh-the-definition-and-importance-of-data-mesh), а также как реализовать data mesh в <Constant name="cloud" />:

- Настроить базовый (foundational) проект под названием “Jaffle | Data Analytics”
- Настроить downstream‑проект под названием “Jaffle | Finance”
- Добавить доступ к моделям, версии и контракты
- Настроить задание <Constant name="cloud" />, которое запускается по завершении upstream‑задания

Подробнее о том, почему data mesh важен, читайте в статье: [What is data mesh? The definition and importance of data mesh](https://www.getdbt.com/blog/what-is-data-mesh-the-definition-and-importance-of-data-mesh).

Для получения дополнительной информации о важности data mesh, прочитайте этот пост: [Что такое data mesh? Определение и важность data mesh](https://www.getdbt.com/blog/what-is-data-mesh-the-definition-and-importance-of-data-mesh).

:::tip Видео для вас
Вы можете бесплатно ознакомиться с [Основами dbt](https://learn.getdbt.com/courses/dbt-fundamentals), если вас интересует обучение с видео.

Вы также можете посмотреть [видео на YouTube о dbt и Snowflake](https://www.youtube.com/watch?v=kbCkwhySV_I&list=PL0QYlrC86xQm7CoOH6RS7hcgLnd3OQioG).
:::

### Связанные материалы:
- [Концепции data mesh: что это такое и как начать работу](https://www.getdbt.com/blog/data-mesh-concepts-what-it-is-and-how-to-get-started)
- [Как определить структуру вашего <Constant name="mesh" />](/best-practices/how-we-mesh/mesh-3-structures)
- [Руководство по лучшим практикам <Constant name="mesh" />](/best-practices/how-we-mesh/mesh-4-implementation)
- [Часто задаваемые вопросы по <Constant name="mesh" />](/best-practices/how-we-mesh/mesh-5-faqs)

## Предварительные требования

Чтобы воспользоваться возможностями <Constant name="mesh" />, необходимо следующее:

- У вас должен быть аккаунт уровня Enterprise в [<Constant name="cloud" />](https://www.getdbt.com/get-started/enterprise-contact-pricing) <Lifecycle status="managed,managed_plus" />
- У вас есть доступ к облачной платформе данных, права на загрузку таблиц с примером данных, а также права в <Constant name="cloud" /> на создание новых проектов.
- В этом руководстве используются примерные данные Jaffle Shop, включая таблицы `customers`, `orders` и `payments`. Следуйте приведённым инструкциям, чтобы загрузить эти данные в соответствующую платформу данных:
  - [Snowflake](/guides/snowflake?step=3)
  - [Databricks](/guides/databricks?step=3)
  - [Redshift](/guides/redshift?step=3)
  - [BigQuery](/guides/bigquery?step=3)
  - [Fabric](/guides/microsoft-fabric?step=2)
  - [Starburst Galaxy](/guides/starburst-galaxy?step=2)

Это руководство предполагает, что у вас есть опыт работы с dbt или базовые знания о нем. Пройдите курс [Основы dbt](https://learn.getdbt.com/courses/dbt-fundamentals), если вы новичок в dbt.

## Создание и настройка двух проектов

В этом разделе вы создадите два новых пустых проекта в <Constant name="cloud" />, которые будут использоваться в качестве базового и зависимого (downstream) проектов.

- **Основные проекты** (или upstream проекты) обычно содержат основные модели и наборы данных, которые служат основой для дальнейшего анализа и отчетности.
- **Downstream проекты** строятся на этих основах, часто добавляя более специфические преобразования или бизнес-логику для выделенных команд или целей.

Например, всегда предприимчивый и вымышленный аккаунт "Jaffle Labs" создаст два проекта для своей команды по аналитике данных и финансовой команды: Jaffle | Data Analytics и Jaffle | Finance.

<Lightbox src="/img/guides/dbt-mesh/project_names.png" width="50%" title="Create two new dbt projects named 'Jaffle | Data Analytics' and 'Jaffle Finance' " />

Чтобы [создать](/docs/cloud/about-cloud-setup) новый проект в <Constant name="cloud" />:

1. В **Account settings** перейдите в **Projects**. Нажмите **New project**.
2. Введите имя проекта и нажмите **Continue**.
   - Используйте «Jaffle | Data Analytics» для одного проекта
   - Используйте «Jaffle | Finance» для второго проекта
3. Выберите вашу платформу данных, затем нажмите **Next**, чтобы настроить подключение.
4. В разделе **Configure your environment** введите **Settings** для нового проекта.
5. Нажмите **Test Connection**. Это проверит, что <Constant name="cloud" /> может получить доступ к вашей учетной записи платформы данных.
6. Нажмите **Next**, если проверка прошла успешно. Если она завершилась неудачно, возможно, потребуется вернуться назад и перепроверить настройки.
   - Для этого руководства убедитесь, что для каждого проекта создано по одному [development](/docs/dbt-cloud-environments#create-a-development-environment) и одному [Deployment](/docs/deploy/deploy-environments) окружению.
     - Для «Jaffle | Data Analytics» установите базу данных по умолчанию `jaffle_da`.
     - Для «Jaffle | Finance» установите базу данных по умолчанию `jaffle_finance`.
7. Следуйте дальнейшим подсказкам, чтобы завершить настройку проекта. После конфигурации каждый проект должен иметь:
    - Подключение к платформе данных
    - Новый git-репозиторий
    - Одно или несколько [environments](/docs/deploy/deploy-environments) (например, development, deployment)

<DocCarousel slidesPerView={1}>

<Lightbox src="/img/guides/dbt-ecosystem/dbt-python-snowpark/5-development-schema-name/1-settings-gear-icon.png" width="90%" title="Navigate to Account settings." />

<Lightbox src="/img/guides/dbt-mesh/select_projects.png" width="30%" title="Select projects from the menu." />

<Lightbox src="/img/guides/dbt-mesh/create_a_new_project.png" width="95%" title="Create a new project in the Studio IDE." />

<Lightbox src="/img/guides/dbt-mesh/enter_project_name.png" width="95%" title="Name your project." />

<Lightbox src="/img/guides/dbt-mesh/select_a_connection.png" width="95%" title="Select the relevant connection for your projects." />

</DocCarousel>

### Создание production-окружения
В <Constant name="cloud" /> каждый проект может иметь одно deployment-окружение, помеченное как «Production». Для каждого проекта, который вы хотите объединить в «mesh», необходимо настроить ["Production" или "Staging" deployment environment](/docs/deploy/deploy-environments). Это позволит вам использовать <Constant name="explorer" /> на [последующих шагах](/guides/mesh-qs?step=5#create-and-run-a-dbt-cloud-job) данного руководства.

Чтобы установить производственную среду:
1. Перейдите в **Развертывание** -> **Среды**, затем нажмите **Создать новую среду**.
2. Выберите **Развертывание** как тип среды.
3. В разделе **Установить тип развертывания** выберите кнопку **Производственная**.
4. Выберите версию dbt.
5. Продолжайте заполнять поля по мере необходимости в разделах **Подключение развертывания** и **Учетные данные развертывания**.
6. Нажмите **Тестировать подключение**, чтобы подтвердить подключение развертывания.
6. Нажмите **Сохранить**, чтобы создать производственную среду.

<Lightbox src="/img/docs/dbt-cloud/using-dbt-cloud/prod-settings-1.png" width="100%" title="Установите вашу производственную среду как среду по умолчанию в настройках среды"/>

## Настройка основного проекта

Этот upstream проект — это место, где вы создаете свои основные данные. Этот проект будет содержать исходные данные, модели промежуточной обработки и основную бизнес-логику.

dbt Cloud позволяет специалистам по данным разрабатывать в своем любимом инструменте и оснащен локальным [dbt Cloud CLI](/docs/cloud/cloud-cli-installation) или браузерным [dbt Cloud IDE](/docs/cloud/dbt-cloud-ide/develop-in-the-cloud).

<Constant name="cloud" /> позволяет специалистам по данным разрабатывать проекты в привычных им инструментах и поставляется с локальным [<Constant name="cloud" /> CLI](/docs/cloud/cloud-cli-installation) или браузерной средой [<Constant name="cloud_ide" />](/docs/cloud/studio-ide/develop-in-studio).

В этом разделе руководства вы настроите проект **"Jaffle | Data Analytics"** как базовый проект, используя <Constant name="cloud_ide" />.

1. Сначала перейдите на страницу **Develop**, чтобы проверить, что всё настроено корректно.
2. Нажмите **Initialize dbt project**, если вы начали с пустого репозитория.
3. Удалите папку `models/example`.  
4. Перейдите к файлу `dbt_project.yml` и переименуйте проект (строка 5) с `my_new_project` на `analytics`.
5. В файле `dbt_project.yml` удалите строки 39–42 (ссылку на модель `my_new_project`).
6. В **File <Constant name="explorer" />** наведите курсор на директорию проекта и нажмите **...**, затем выберите **Create file**.
7. Создайте две новые папки: `models/staging` и `models/core`.

1. Создайте новый YAML файл `models/staging/sources.yml`.
2. Объявите источники, скопировав следующее в файл и нажав **Сохранить**.

  <File name='models/staging/sources.yml'>

  ```yaml

  sources:
    - name: jaffle_shop
      description: Это реплика базы данных Postgres, используемой нашим приложением
      database: raw
      schema: jaffle_shop
      tables:
        - name: customers
          description: Одна запись на клиента.
        - name: orders
          description: Одна запись на заказ. Включает отмененные и удаленные заказы.
  ```
  </File>

3. Создайте файл `models/staging/stg_customers.sql`, чтобы выбрать из таблицы `customers` в источнике `jaffle_shop`.

  <File name='models/staging/stg_customers.sql'>

  ```sql
  select
      id as customer_id,
      first_name,
      last_name

  from {{ source('jaffle_shop', 'customers') }}
  ```
  </File>

4. Создайте файл `models/staging/stg_orders.sql`, чтобы выбрать из таблицы `orders` в источнике `jaffle_shop`.

  <File name='models/staging/stg_orders.sql'>

  ```sql
  select
      id as order_id,
      user_id as customer_id,
      order_date,
      status

  from {{ source('jaffle_shop', 'orders') }}
  ```
  </File>

5. Создайте файл `models/core/fct_orders.sql`, чтобы построить таблицу фактов с деталями клиентов и заказов.

  <File name='models/core/fct_orders.sql'>

  ```sql
  with customers as (
      select * 
      from {{ ref('stg_customers') }}
  ),

  orders as (
      select * 
      from {{ ref('stg_orders') }}
  ),

  customer_orders as (
      select
          customer_id,
          min(order_date) as first_order_date
      from orders
      group by customer_id
  ),

  final as (
      select
          o.order_id,
          o.order_date,
          o.status,
          c.customer_id,
          c.first_name,
          c.last_name,
          co.first_order_date,
          -- Обратите внимание, что мы использовали макрос для этого, чтобы соответствующий синтаксис DATEDIFF использовался для каждой соответствующей платформы данных
          {{ datediff('first_order_date', 'order_date', 'day') }} as days_as_customer_at_purchase
      from orders o
      left join customers c using (customer_id)
      left join customer_orders co using (customer_id)
  )

  select * from final
  ```
  </File>

6. Перейдите в **Командную строку** и выполните `dbt build`.

Прежде чем команда, работающая с зависимыми проектами (downstream), сможет использовать ресурсы из этого базового проекта, необходимо сначала:

- [Создать и определить](/docs/mesh/govern/model-access) как минимум одну модель со статусом “public”
- Успешно выполнить [deployment job](/docs/deploy/deploy-jobs)
  - Обратите внимание: включите переключатель **Generate docs on run** для этой job, чтобы обновить <Constant name="explorer" />. После выполнения вы сможете нажать Explore в верхнем меню и увидеть корректно отображающиеся lineage, тесты и документацию.

## Определите публичную модель и запустите первую задачу

В предыдущем разделе вы упорядочили свои базовые строительные блоки, теперь давайте интегрируем <Constant name="mesh" />.

Хотя финансовая команда требует модель `fct_orders` для анализа тенденций платежей, другие модели, особенно те, которые находятся в промежуточном слое и используются для очистки данных и объединения, не нужны downstream командам.

Чтобы сделать `fct_orders` общедоступной:

1. В файле `models/core/core.yml` добавьте `access: public` к соответствующему YAML файлу, добавив и сохранив следующее:

  <File name='models/core/core.yml'>

  ```yaml

  models:
    - name: fct_orders
      config:
        access: public # изменено на config в версии v1.10
      description: "Детали по клиентам и заказам"
      columns:
        - name: order_id
          data_type: number
          description: ""

        - name: order_date
          data_type: date
          description: ""

        - name: status
          data_type: varchar
          description: "Указывает статус заказа"

        - name: customer_id
          data_type: number
          description: ""

        - name: first_name
          data_type: varchar
          description: ""

        - name: last_name
          data_type: varchar
          description: ""

        - name: first_order_date
          data_type: date
          description: ""

        - name: days_as_customer_at_purchase
          data_type: number
          description: "Дни между этой покупкой и первой покупкой клиента"
  ```
  </File>

Примечание: по умолчанию доступ к моделям установлен как `protected`, что означает, что на них можно ссылаться только внутри того же проекта. Подробнее о типах доступа и группах моделей см. [здесь](/docs/mesh/govern/model-access#access-modifiers).

2. Перейдите на вкладку <Constant name="cloud_ide" /> **Lineage**, чтобы увидеть, что модель помечена как **Public** под её именем.

<Lightbox src="/img/guides/dbt-mesh/da_lineage.png" title="Родословная Jaffle | Data Analytics" />

3. Перейдите в **Контроль версий** и нажмите кнопку **Commit and Sync**, чтобы зафиксировать ваши изменения.
4. Объедините ваши изменения в основную или производственную ветку.

### Создание и запуск dbt‑job

Прежде чем команды‑потребители смогут использовать ассеты из этого базового проекта, необходимо [создать production‑окружение](/guides/mesh-qs?step=3#create-a-production-environment) и успешно запустить [deployment job](/docs/deploy/deploy-jobs).

Чтобы запустить свой первый deployment‑job в <Constant name="cloud" />, вам нужно создать новый job в <Constant name="cloud" />.  
1. Перейдите в **Orchestration** > **Jobs**.  
2. Нажмите **Create job**, затем выберите **Deploy job**.  
3. Включите опцию **Generate docs on run**. Это позволит заполнить метаданные в <Constant name="explorer"/>.

<Lightbox src="/img/guides/dbt-mesh/generate_docs_on_run.png" width="75%" title=" Select the 'Generate docs on run' option when configuring your dbt job." />

4. Нажмите **Save**.  
5. Нажмите **Run now**, чтобы запустить job.  
6. После завершения выполнения перейдите в <Constant name="explorer"/>. Теперь вы должны увидеть lineage, тесты и документацию, успешно отображающиеся в системе.

Подробнее о том, как <Constant name="cloud" /> использует метаданные из Staging‑окружения для разрешения ссылок в downstream‑проектах, см. раздел [Staging with downstream dependencies](/docs/mesh/govern/project-dependencies#staging-with-downstream-dependencies).

## Ссылка на публичную модель в вашем downstream проекте

В этом разделе вы настроите downstream‑проект **Jaffle | Finance** и с помощью [cross-project reference](/docs/mesh/govern/project-dependencies) подключите модель `fct_orders` из базового (foundational) проекта. Перейдите на страницу **Develop**, чтобы настроить наш проект:

1. Если вы также начали с нового git‑репозитория, нажмите **Initialize dbt project** в разделе **Version control**.
2. Удалите папку `models/example`.
3. Перейдите к файлу `dbt_project.yml` и переименуйте проект (строка 5) с `my_new_project` на `finance`.
4. Перейдите к файлу `dbt_project.yml` и удалите строки 39–42 (ссылку на модель `my_new_project`).
5. В **File <Constant name="explorer" />** наведите курсор на директорию проекта, нажмите **...** и выберите **Create file**.
6. Назовите файл `dependencies.yml`.
7. Добавьте upstream‑проект `analytics` и пакет `dbt_utils`. Нажмите **Save**.

  <File name='dependencies.yml'>

  ```yaml

  packages:
    - package: dbt-labs/dbt_utils
      version: 1.1.1

  projects:
    - name: analytics

  ```
  </File>

### Промежуточный слой

Теперь, когда вы настроили основной проект, давайте начнем строить данные. Настройте промежуточный слой следующим образом:

1. Создайте новый YAML файл `models/staging/sources.yml` и объявите источники, скопировав следующее в файл и нажав **Сохранить**.

    <File name='models/staging/sources.yml'>

    ```yml

    sources:
      - name: stripe
        database: raw
        schema: stripe 
        tables:
          - name: payment
    ```

    </File>

2. Создайте `models/staging/stg_payments.sql`, чтобы выбрать из таблицы `payment` в источнике `stripe`.

    <File name='models/staging/stg_payments.sql'>

    ```sql

    with payments as (
        select * from {{ source('stripe', 'payment') }}
    ),

    final as (
        select 
            id as payment_id,
            orderID as order_id,
            paymentMethod as payment_method,
            amount,
            created as payment_date 
        from payments
    )

    select * from final

    ```

    </File>

### Ссылка на публичную модель

Теперь вы готовы добавить модель, которая исследует, как типы платежей варьируются на протяжении всего пути клиента. Это помогает определить, уменьшается ли использование подарочных карт с купонами с повторными покупками, как ожидает наша маркетинговая команда, или остается постоянным.

1. Чтобы сделать ссылку на модель, используйте следующую логику, чтобы выяснить это:

    <File name='models/core/agg_customer_payment_journey.sql'>

    ```sql

    with stg_payments as (
        select * from {{ ref('stg_payments') }}
    ),

    fct_orders as (
        select * from {{ ref('analytics', 'fct_orders') }}
    ),

    final as (
        select 
            days_as_customer_at_purchase,
            -- мы используем макрос pivot в пакете dbt_utils, чтобы создать столбцы, которые суммируют платежи для каждого метода
            {{ dbt_utils.pivot(
                'payment_method',
                dbt_utils.get_column_values(ref('stg_payments'), 'payment_method'),
                agg='sum',
                then_value='amount',
                prefix='total_',
                suffix='_amount'
            ) }}, 
            sum(amount) as total_amount
        from fct_orders
        left join stg_payments using (order_id)
        group by 1
    )

    select * from final

    ```

    </File> 

2. Обратите внимание, как работает межпроектный `ref`! Когда вы добавляете `ref`, функция автодополнения в <Constant name="cloud_ide" /> распознаёт публичную модель как доступную.  
<Lightbox src="/img/guides/dbt-mesh/cross_proj_ref_autocomplete.png" title="Cross-project ref autocomplete in the Studio IDE" />

3. Это автоматически разрешает (или связывает) с правильной базой данных, схемой и таблицей/представлением, установленными upstream проектом.
<Lightbox src="/img/guides/dbt-mesh/cross_proj_ref_compile.png" title="Компиляция кросс-проектной ссылки" />

4. Вы также можете увидеть это соединение, отображаемое в живой вкладке **Родословная**.
<Lightbox src="/img/guides/dbt-mesh/cross_proj_ref_lineage.png" title="Родословная кросс-проектной ссылки" />

## Добавление версий моделей и контрактов

Как вы можете повысить устойчивость и добавить защитные механизмы в этот тип многопроектных отношений? Вы можете принять лучшие практики из программной инженерии, такие как:

1. **Определение контрактов моделей** &mdash; Настройте [контракты моделей](/docs/mesh/govern/model-contracts) в dbt, чтобы задать набор предварительных «гарантий», определяющих структуру вашей модели. В процессе сборки dbt проверит, что трансформация модели формирует набор данных, соответствующий контракту; если это не так, сборка завершится ошибкой.
2. **Определение версий моделей** &mdash; Используйте [версии моделей](/docs/mesh/govern/model-versions) для систематического управления обновлениями и обработки ломающих изменений.

### Настройка контрактов моделей
Как часть команды по аналитике данных, вы можете захотеть убедиться, что модель `fct_orders` надежна для downstream пользователей, таких как финансовая команда.

1. Перейдите в `models/core/core.yml` и под моделью `fct_orders` перед разделом `columns:` добавьте контракт данных для обеспечения надежности:

```yaml
    models:
      - name: fct_orders
    description: "Сведения о клиентах и заказах"
    config:
      access: public # changed to config in v1.10
      contract:
        enforced: true
    columns:
      - name: order_id
        .....
```

2. Проверьте, что произойдет, если этот контракт будет нарушен. В `models/core/fct_orders.sql` закомментируйте столбец `orders.status` и нажмите **Сборка**, чтобы попытаться построить модель.
   - Если контракт нарушен, сборка завершится неудачей, как видно из истории командной строки.
    <Lightbox src="/img/guides/dbt-mesh/break_contract.png" title="Контракт данных был нарушен, и выполнение сборки dbt завершилось неудачей." />

### Настройка версий моделей
В этом разделе вы настроите версии моделей командой по аналитике данных, когда они обновляют модель `fct_orders`, предлагая при этом обратную совместимость и уведомление о миграции для downstream финансовой команды.

1. Переименуйте существующий файл модели с `models/core/fct_orders.sql` на `models/core/fct_orders_v1.sql`.
2. Создайте новый файл `models/core/fct_orders_v2.sql` и измените схему:
   - Закомментируйте `o.status` в `final` CTE.
   - Добавьте новое поле, `case when o.status = 'returned' then true else false end as is_return`, чтобы указать, был ли заказ возвращен.
3. Затем добавьте следующее в ваш файл `models/core/core.yml`:
   - Столбец `is_return`
   - Две версии модели
   - `latest_version`, чтобы указать, какая модель является последней (и должна использоваться по умолчанию, если не указано иное)
   - `deprecation_date` для версии 1, чтобы указать, когда модель будет устаревшей.

4. Теперь он должен выглядеть следующим образом:

<File name='models/core/core.yml'>

```yaml


models:
  - name: fct_orders
    description: "Детали клиентов и заказов"
    latest_version: 2
    config:
      access: public # changed to config in v1.10
      contract:
        enforced: true
    columns:
      - name: order_id
        data_type: number
        description: ""

      - name: order_date
        data_type: date
        description: ""

      - name: status
        data_type: varchar
        description: "Указывает статус заказа"

      - name: is_return
        data_type: boolean
        description: "Указывает, был ли заказ возвращен"

      - name: customer_id
        data_type: number
        description: ""

      - name: first_name
        data_type: varchar
        description: ""

      - name: last_name
        data_type: varchar
        description: ""

      - name: first_order_date
        data_type: date
        description: ""

      - name: days_as_customer_at_purchase
        data_type: number
        description: "Дни между этой покупкой и первой покупкой клиента"

    # Объявите версии и выделите различия
    versions:
    
      - v: 1
        deprecation_date: 2024-06-30 00:00:00.00+00:00
        columns:
          # Это означает: используйте список 'columns' из выше, но исключите is_return
          - include: all
            exclude: [is_return]
        
      - v: 2
        columns:
          # Это означает: используйте список 'columns' из выше, но исключите status
          - include: all
            exclude: [status]
```

</File>

5. Проверьте, как dbt компилирует оператор `ref` на основе обновлений. Откройте новый файл, добавьте следующие операторы select и нажмите **Компиляция**. Обратите внимание, как каждая ссылка компилируется в указанную версию (или последнюю версию, если не указано).

```sql
select * from {{ ref('fct_orders', v=1) }}
select * from {{ ref('fct_orders', v=2) }}
select * from {{ ref('fct_orders') }}
```

## Добавление dbt job в downstream‑проекте

Прежде чем продолжить, убедитесь, что вы закоммитили и смержили изменения в обоих проектах — “Jaffle | Data Analytics” и “Jaffle | Finance”.

Участник команды Finance хочет запланировать задание <Constant name="cloud" />, чтобы оно запускалось для анализа customer payment journey сразу после того, как команда data analytics обновит свои пайплайны.

1. В проекте “Jaffle | Finance” перейдите на страницу **Jobs**, выбрав **Orchestration** > **Jobs**.  
2. Нажмите **Create job**, затем **Deploy job**.  
3. Укажите имя задания, затем прокрутите страницу вниз до раздела **Job completion**.  
4. В разделе **Triggers** настройте задание так, чтобы оно **Run when another job finishes**, и выберите upstream‑job из проекта “Jaffle | Data Analytics”.

<Lightbox src="/img/guides/dbt-mesh/trigger_on_completion.png" title="Trigger job on completion" />

5. Нажмите **Сохранить** и убедитесь, что задача настроена правильно.
6. Перейдите на страницу задач “Jaffle | Data Analytics”. Выберите **Ежедневная задача** и нажмите **Запустить сейчас**. 
7. После успешного завершения этой задачи вернитесь на страницу задач “Jaffle | Finance”. Вы должны увидеть, что задача финансовой команды была запущена автоматически.

Это упрощает процесс синхронизации с upstream таблицами и устраняет необходимость в более сложных навыках оркестрации, таких как координация задач между проектами через внешний оркестратор.

## Просмотр предупреждения об устаревании

Чтобы узнать, сколько времени у финансовой команды есть на миграцию с `fct_orders_v1` на `fct_orders_v2`, выполните следующие шаги:

1. В проекте “Jaffle | Finance” перейдите на страницу **Разработка**.
2. Измените кросс-проектную ссылку, чтобы использовать v=1 в `models/marts/agg_customer_payment_journey.sql`:

<File name='models/core/agg_customer_payment_journey.sql'>

```sql

with stg_payments as (
    select * from {{ ref('stg_payments') }}
),

fct_orders as (
    select * from {{ ref('analytics', 'fct_orders', v=1) }}
),

final as (
    select 
        days_as_customer_at_purchase,
        -- мы используем макрос pivot в пакете dbt_utils, чтобы создать столбцы, которые суммируют платежи для каждого метода
        {{ dbt_utils.pivot(
            'payment_method',
            dbt_utils.get_column_values(ref('stg_payments'), 'payment_method'),
            agg='sum',
            then_value='amount',
            prefix='total_',
            suffix='_amount'
        ) }}, 
        sum(amount) as total_amount
    from fct_orders
    left join stg_payments using (order_id)
    group by 1
)

select * from final
```

</File>

3. В <Constant name="cloud_ide" /> перейдите в раздел **Version control**, чтобы закоммитить и смержить изменения.
4. Перейдите на страницу **Deploy**, затем **Jobs**.
5. Нажмите **Run now**, чтобы запустить job Finance. Модель `agg_customer_payment_journey` будет собрана, и при этом отобразится предупреждение о дате депрекации.

<Lightbox src="/img/guides/dbt-mesh/deprecation_date_warning.png" title="Модель отобразит предупреждение о дате устаревания." />

## Просмотр lineage с помощью dbt Catalog

Используйте [<Constant name="explorer" />](/docs/explore/explore-projects), чтобы просматривать lineage между проектами в <Constant name="cloud" />. Перейдите на страницу **Explore** для каждого из ваших проектов — теперь вы сможете [видеть lineage бесшовно между проектами](/docs/explore/explore-multiple-projects).

<Lightbox src="/img/guides/dbt-mesh/jaffle_da_final_lineage.png" width="85%" title="Просмотр lineage «Jaffle | Data Analytics» с помощью dbt Catalog" />

## Что дальше

<ConfettiTrigger>

Поздравляем 🎉! Теперь вы готовы внедрить преимущества <Constant name="mesh" /> в своей организации. Вы узнали:

- Как создать базовый проект «Jaffle | Data Analytics».
- Как создать downstream‑проект «Jaffle | Finance».
- Как реализовать доступ к моделям, версии и контракты.
- Как настроить задания <Constant name="cloud" />, которые запускаются по завершении upstream‑заданий.

Вот некоторые дополнительные ресурсы, которые помогут вам продолжить ваше путешествие:

- [Как мы строим проекты dbt mesh](/best-practices/how-we-mesh/mesh-1-intro)
- [Часто задаваемые вопросы по <Constant name="mesh" />](/best-practices/how-we-mesh/mesh-5-faqs)
- [Внедрение <Constant name="mesh" /> с помощью <Constant name="semantic_layer" />](/docs/use-dbt-semantic-layer/sl-faqs#how-can-i-implement-dbt-mesh-with-the-dbt-semantic-layer)
- [Межпроектные ссылки](/docs/mesh/govern/project-dependencies#how-to-write-cross-project-ref)
- [<Constant name="explorer" />](/docs/explore/explore-projects)

</ConfettiTrigger>

</div>