---
title: 'Переход с dbt Core на платформу dbt: советы по оптимизации'
id: core-to-cloud-3
description: "Используйте это руководство, чтобы узнать, как оптимизировать работу с dbt и получить ответы на распространённые вопросы."
hoverSnippet: "Используйте это руководство, чтобы узнать, как оптимизировать работу с dbt и получить ответы на распространённые вопросы."
icon: 'guides'
hide_table_of_contents: true
tags: ['Migration','dbt Core','dbt platform']
keywords: ['dbt Core','dbt platform','Migration', 'Move dbt', 'Migrate dbt']
level: 'Intermediate'
---

## Введение {#introduction}

Переход с <Constant name="core" /> на <Constant name="cloud" /> упрощает рабочие процессы аналитической инженерии, позволяя командам разрабатывать, тестировать, развёртывать и исследовать продукты данных в рамках единого, полностью управляемого программного сервиса.

Изучите нашу серию из трёх руководств о переходе с <Constant name="core" /> на <Constant name="cloud" />. Эта серия идеально подойдёт пользователям, которые стремятся к более упорядоченным рабочим процессам и расширенным возможностям аналитики:

import CoretoCloudTable from '/snippets/_core-to-cloud-guide-table.md';

<CoretoCloudTable/>

## Что вы узнаете {#what-youll-learn}

Возможно, вы уже начали переход на <Constant name="cloud" /> и ищете советы, которые помогут оптимизировать работу с <Constant name="cloud" />. В этом руководстве собраны рекомендации и важные моменты, на которые стоит обратить внимание, в следующих областях:

- [Адаптеры и подключения](/guides/core-to-cloud-3?step=3)  
- [Инструменты разработки](/guides/core-to-cloud-3?step=4)  
- [Оркестрация](/guides/core-to-cloud-3?step=5)
- [<Constant name="mesh" />](/guides/core-to-cloud-3?step=6)
- [<Constant name="semantic_layer" />](/guides/core-to-cloud-3?step=7)
- [<Constant name="explorer" />](/guides/core-to-cloud-3?step=8)

## Адаптеры и подключения {#adapters-and-connections}

В <Constant name="cloud" /> вы можете нативно подключиться к своей платформе данных и проверить её [соединение](/docs/connect-adapters) одним нажатием кнопки. Это особенно полезно для пользователей, которые только начинают работать с <Constant name="cloud" /> или хотят упростить процесс настройки подключения. Ниже приведены несколько советов и нюансов, которые стоит учитывать:

### Советы {#tips}
- Управляйте [версиями dbt](/docs/dbt-versions/upgrade-dbt-version-in-cloud) и обеспечивайте совместную работу команды с помощью функции one-click в <Constant name="cloud" />, избавляясь от необходимости ручных обновлений и расхождений версий. Выберите [release track](/docs/dbt-versions/cloud-release-tracks) для получения регулярных обновлений, чтобы всегда быть в курсе исправлений и (опционально) получать ранний доступ к новой функциональности для вашего dbt‑проекта.
- <Constant name="cloud" /> поддерживает широкий набор [cloud providers](/docs/cloud/connect-data-platform/about-connections), включая Snowflake, Databricks, BigQuery, Fabric и Redshift (и это лишь некоторые из них).
- Используйте [Extended Attributes](/docs/deploy/deploy-environments#extended-attributes), чтобы задать гибкий фрагмент [profiles.yml](/docs/core/connect-data-platform/profiles.yml) в настройках окружения <Constant name="cloud" />. Это даёт больше контроля над окружениями (как для деплоя, так и для разработки) и расширяет возможности того, как <Constant name="cloud" /> подключается к платформе данных в рамках конкретного окружения.
  - Например, если у вас есть поле в `profiles.yml`, которое вы хотели бы добавить в пользовательский интерфейс адаптера <Constant name="cloud" />, вы можете настроить его с помощью Extended Attributes.

### Предостережения {#caveats}
- Не все параметры доступны для адаптеров.
- Проект может использовать только один тип хранилища.

## Инструменты разработки {#development-tools}

<Constant name="cloud" /> предоставляет специалистам по данным возможность разрабатывать в том инструменте, который им удобнее. Он поставляется с [<Constant name="cloud" /> CLI](/docs/cloud/cloud-cli-installation) (локально) или [<Constant name="cloud_ide" />](/docs/cloud/studio-ide/develop-in-studio) (в браузере) для сборки, тестирования, запуска и версионирования ваших dbt‑проектов.

Оба инструмента для разработки адаптированы под разные аудитории и предпочтения внутри команды. Чтобы упростить рабочие процессы, важно понимать, кому больше подойдет <Constant name="cloud_ide" />, а кто, вероятно, выберет <Constant name="cloud" /> CLI. В этом разделе объясняются эти различия и предпочтения.

### Studio IDE {#studio-ide}
Веб‑интерфейс для сборки, тестирования, запуска и версионирования dbt‑проектов. Он компилирует dbt‑код в SQL и выполняет его напрямую в вашей базе данных. <Constant name="cloud_ide" /> делает процесс разработки быстрым и удобным как для новых, так и для опытных специалистов по данным при создании и тестировании изменений.

**Кому может подойти <Constant name="cloud_ide" />?**

- Новым пользователям dbt или тем, кто переходит с других инструментов и ценит более направляемый опыт работы через браузерный интерфейс.
- Членам команды, для которых важны скорость и удобство при старте с новым или уже существующим проектом.
- Пользователям, которые ценят мгновенную обратную связь от <Constant name="cloud_ide" />, например отображение несохранённых изменений.

**Ключевые особенности**

- В <Constant name="cloud_ide" /> упрощена функциональность <Constant name="git" />:
  - Создание feature-веток на основе ветки, настроенной в среде разработки.
  - Просмотр сохранённых, но ещё не закоммиченных изменений кода прямо в <Constant name="cloud_ide" />.
- [Форматирование или линтинг](/docs/cloud/studio-ide/lint-format) кода с помощью `sqlfluff` или `sqlfmt`. Также поддерживается добавление собственных правил линтинга.
- Позволяет пользователям нативно [использовать defer к production](/docs/cloud/about-cloud-develop-defer#defer-in-dbt-cloud-cli) для метаданных прямо в рабочих процессах разработки, уменьшая количество создаваемых объектов.
- Поддержка одновременного запуска нескольких dbt-команд с помощью [безопасного параллельного выполнения](/reference/dbt-commands#parallel-execution) — это [функция](/docs/cloud/about-cloud/dbt-cloud-features), доступная в инфраструктуре <Constant name="cloud" />. В отличие от этого, `dbt-core` *не поддерживает* безопасное параллельное выполнение нескольких вызовов в рамках одного процесса.

<Constant name="cloud_ide" /> предоставляет упрощённый интерфейс, доступный всем пользователям независимо от их технической подготовки. Однако существуют возможности, которые намеренно недоступны в <Constant name="cloud_ide" />, поскольку основной акцент сделан на простоте и удобстве использования:

- Предварительная проверка для автоматических проверок перед *фиксацией* кода пока недоступна.
- Массовое создание файлов / взаимодействие с файловой системой недоступно.
- Комбинирование/передача команд, таких как `dbt run -s (bash command)`, недоступно.

### dbt CLI {#dbt-cli}
CLI <Constant name="cloud" /> позволяет запускать [команды](/reference/dbt-commands#available-commands) dbt для вашей среды разработки <Constant name="cloud" /> прямо из локальной командной строки. Этот вариант подходит пользователям, которым нужен полный контроль над средой разработки, и тем, кто уверенно работает с командной строкой.

При переходе с dbt Core на <Constant name="cloud" /> обязательно проверьте, что файл `.gitignore` содержит [необходимые папки](/docs/cloud/git/version-control-basics#the-gitignore-file). dbt Core не взаимодействует с git, поэтому <Constant name="cloud" /> не добавляет и не проверяет записи в файле `.gitignore` автоматически. Кроме того, если репозиторий уже содержит код dbt и не требует инициализации, <Constant name="cloud" /> не будет добавлять отсутствующие записи в файл `.gitignore`.

**Кому может подойти CLI <Constant name="cloud" />?**

- Специалистам по данным, привыкшим работать с определённым набором инструментов разработки.
- Пользователям, которым нужен детальный контроль над процессами <Constant name="git" /> (например, pre-commit‑хуки для автоматических проверок перед коммитом кода).
- Специалистам по данным, которым необходимо выполнять сложные операции, такие как массовая генерация файлов или специфические комбинации команд.

**Ключевые особенности**

- Позволяет пользователям запускать команды dbt в своей среде разработки <Constant name="cloud" /> из локальной командной строки с минимальной настройкой.
- Позволяет пользователям нативно [defer to production](/docs/cloud/about-cloud-develop-defer#defer-in-dbt-cloud-cli) метаданные непосредственно в рабочих процессах разработки, сокращая количество объектов.
- Поддерживает одновременный запуск нескольких команд dbt с помощью [safe parallel execution](/reference/dbt-commands#parallel-execution) — [функции](/docs/cloud/about-cloud/dbt-cloud-features), доступной в инфраструктуре <Constant name="cloud" />. В отличие от этого, `dbt-core` *не поддерживает* безопасное параллельное выполнение для нескольких запусков в рамках одного процесса.
- Позволяет использовать расширения Visual Studio (VS) Code

## Оркестрация {#orchestration}

<Constant name="cloud" /> предоставляет мощные возможности оркестрации, которые позволяют с лёгкостью планировать, запускать и отслеживать dbt‑задачи. Ниже приведены несколько советов и предостережений, которые стоит учитывать при использовании функций оркестрации в <Constant name="cloud" />:

### Советы {#tips-1}

- Включите [partial parsing](/docs/cloud/account-settings#partial-parsing) между джобами в <Constant name="cloud" />, чтобы значительно ускорить парсинг проекта за счёт обработки только изменённых файлов. Это оптимизирует производительность для крупных проектов.
- [Запускайте несколько CI/CD](/docs/deploy/continuous-integration) джобов одновременно — они не будут блокировать продакшн‑запуски. Планировщик джобов автоматически отменяет устаревшие запуски, когда в репозиторий отправляется новый коммит. Это происходит потому, что каждый PR запускается в собственной схеме.
- <Constant name="cloud" /> автоматически [отменяет](/docs/deploy/job-scheduler#run-cancellation-for-over-scheduled-jobs) запланированный запуск, если предыдущий запуск всё ещё выполняется. Это предотвращает ненужные дублирующие выполнения.
- Защитите себя и актуальность ваших данных от сбоев сторонних сервисов, включив [кэширование Git‑репозитория](/docs/cloud/account-settings#git-repository-caching) в <Constant name="cloud" />, которое хранит кэш репозитория проекта. <Lifecycle status="managed,managed_plus" />
- [Связывайте deploy‑джобы](/docs/deploy/deploy-jobs#trigger-on-job-completion) между проектами <Constant name="cloud" />, настроив джоб или используя [Create Job API](/dbt-cloud/api-v2#/operations/Create%20Job). <Lifecycle status="self_service,managed" />
- [Перезапускайте ваши джобы](/docs/deploy/retry-jobs) с начала или с точки сбоя, если выполнение dbt‑джоба завершилось со статусом **`Error.`**

### Предостережения {#caveats-1}
- Чтобы автоматизировать настройку и конфигурацию вашей платформы <Constant name="cloud" />, вы можете хранить конфигурации джобов в виде кода в репозитории:
  - Ознакомьтесь с нашим [Terraform provider.](https://registry.terraform.io/providers/dbt-labs/dbtcloud/latest/docs/resources/job)
  - В качестве альтернативы посмотрите репозиторий [jobs-as-code](https://github.com/dbt-labs/dbt-jobs-as-code) — это инструмент для управления джобами <Constant name="cloud" /> в виде чётко определённого YAML‑файла.
- Пользователи <Constant name="cloud" /> и внешние email‑адреса могут получать уведомления, если джоб завершился с ошибкой, успешно или был отменён. Чтобы получать уведомления о предупреждениях, вы можете создать [webhook‑подписку](/guides/zapier-slack) и отправлять сообщения в Slack.

## dbt Mesh {#dbt-mesh}

[<Constant name="mesh" />](/best-practices/how-we-mesh/mesh-1-intro) помогает организациям со зрелыми и сложными пайплайнами трансформации данных в dbt повысить гибкость и производительность dbt‑проектов. Он позволяет использовать несколько взаимосвязанных dbt‑проектов вместо одного большого монолитного проекта.

Mesh даёт возможность взаимодействовать с разными проектами и моделями и навигировать между ними с помощью [межпроектных зависимостей](/docs/mesh/govern/project-dependencies#how-to-write-cross-project-ref), улучшая совместную работу и управление данными.

Ниже приведены советы и ограничения, которые стоит учитывать при использовании <Constant name="mesh" />:

### Советы {#tips-2}
- Для динамического разрешения [межпроектных ссылок](/docs/mesh/govern/project-dependencies#how-to-write-cross-project-ref) всем разработчикам необходимо работать в <Constant name="cloud" /> (либо с <Constant name="cloud" /> CLI, либо с <Constant name="cloud_ide" />). Межпроектные ссылки не поддерживаются нативно в <Constant name="core" />, за исключением варианта установки исходного кода апстрим‑проектов [в виде пакетов](/docs/build/packages#how-do-i-add-a-package-to-my-project).
- Связывайте модели между проектами для модульного и масштабируемого подхода к развитию проекта и команд.
- Управляйте доступом к вашим dbt‑моделям как внутри проекта, так и между проектами, используя:
  - **[Groups](/docs/mesh/govern/model-access#groups)** — организация узлов в вашем dbt DAG, которые имеют логическую связь, и назначение владельца для всей группы.
  - **[Model access](/docs/mesh/govern/model-access#access-modifiers)** — управление тем, какие другие модели или проекты могут ссылаться на данную модель.
  - **[Model versions](/docs/mesh/govern/model-versions)** — поддержка внедрения и вывода из использования моделей по мере их эволюции.
  - **[Model contracts](/docs/mesh/govern/model-contracts)** — задание чётких ожиданий к форме данных, чтобы изменения данных выше по цепочке dbt или в логике проекта не нарушали работу downstream‑потребителей данных.
- Используйте [dbt-meshify](https://github.com/dbt-labs/dbt-meshify), чтобы ускорить разбиение монолитного проекта на несколько отдельных проектов.

### Предостережения {#caveats-2}
- Чтобы использовать межпроектные ссылки в dbt, каждому dbt‑проекту должен соответствовать ровно один проект <Constant name="cloud" />. Мы настоятельно не рекомендуем определять несколько проектов для одной и той же кодовой базы, даже если вы пытаетесь управлять правами доступа, подключаться к разным хранилищам данных или разделять продакшн и непроизводственные данные. Хотя раньше это было необходимо, такие возможности, как [Staging environments](/docs/dbt-cloud-environments#types-of-environments), RBAC на уровне окружений (_coming soon_) и [Extended attributes](/docs/dbt-cloud-environments#extended-attributes), сделают это ненужным.
- Зависимости между проектами являются однонаправленными, то есть направлены только в одну сторону. Это означает, что dbt проверяет наличие циклов между проектами (циклических зависимостей) и выдаёт ошибку, если они обнаружены. При этом мы рассматриваем возможность в будущем поддерживать двунаправленные зависимости между проектами — dbt по‑прежнему будет проверять циклы на уровне узлов, но позволит циклы на уровне проектов.
- Все пользователи аккаунта могут просматривать публичные метаданные моделей, что помогает быстрее находить дата‑продукты. Это не связано с доступом к самим данным, который контролируется правами в хранилище данных. Для сценариев, где даже метаданные о переиспользуемом дата‑активе являются чувствительными, мы [рассматриваем](https://github.com/dbt-labs/dbt-core/issues/9340) опциональное расширение для защищённых моделей.

Дополнительные вопросы см. в разделе [FAQ по <Constant name="mesh" />](/best-practices/how-we-mesh/mesh-5-faqs).

## Семантический слой dbt {#dbt-semantic-layer}

Используйте [<Constant name="semantic_layer" />](/docs/use-dbt-semantic-layer/dbt-sl), работающий на базе MetricFlow, чтобы создать единое представление бизнес‑метрик и обеспечить их согласованность во всех аналитических инструментах. Ниже приведены советы и ограничения, которые стоит учитывать при работе с <Constant name="semantic_layer" />.

### Советы {#tips-3}
- Определяйте семантические модели и метрики один раз в <Constant name="cloud" /> с помощью [<Constant name="semantic_layer" />](/docs/use-dbt-semantic-layer/dbt-sl) (на базе MetricFlow). Затем переиспользуйте их в различных аналитических платформах, снижая избыточность и количество ошибок.
- Используйте [API <Constant name="semantic_layer" />](/docs/dbt-cloud-apis/sl-api-overview) для запросов метрик в downstream‑инструментах, чтобы получать согласованные и надежные показатели.
- Подключайтесь к различным приложениям для работы с данными — от BI‑инструментов до ноутбуков, таблиц, каталогов данных и других — чтобы выполнять запросы к метрикам. [Доступные интеграции](/docs/cloud-integrations/avail-sl-integrations) включают Tableau, Google Sheets, Hex и другие.
- Используйте [exports](/docs/use-dbt-semantic-layer/exports), чтобы по расписанию записывать часто используемые запросы напрямую в вашу data‑платформу.

### Ограничения {#caveats-3}
- В настоящее время <Constant name="semantic_layer" /> поддерживает выполнение запросов только в окружении Deployment. Поддержка запросов в Development появится позже.
- Запросы и команды семантического слоя можно выполнять в CLI <Constant name="cloud" />, однако выполнение запросов/команд семантического слоя в <Constant name="cloud_ide" /> пока *не поддерживается*.
- <Constant name="semantic_layer" /> не поддерживает использование [Single sign-on (SSO)](/docs/cloud/manage-access/sso-overview) для [production credentials <Constant name="semantic_layer" />](/docs/dbt-cloud-apis/service-tokens#permissions-for-service-account-tokens). При этом SSO поддерживается для пользовательских аккаунтов в среде разработки.

Дополнительную информацию см. в разделе [FAQ по <Constant name="semantic_layer" />](/docs/use-dbt-semantic-layer/sl-faqs).

## dbt Catalog {#dbt-catalog}

[<Constant name="explorer" />](/docs/explore/explore-projects) расширяет ваши возможности по поиску, пониманию и отладке дата-активов за счёт богатых метаданных и визуализации lineage. Ниже приведены рекомендации и ограничения, которые стоит учитывать при использовании <Constant name="explorer" />.

### Советы {#tips-4}
- Используйте возможности поиска и фильтрации в <Constant name="explorer" />, чтобы быстро находить модели, источники и тесты, тем самым упрощая рабочий процесс.
- Просматривайте все [разные проекты](/docs/explore/explore-multiple-projects) и публичные модели в аккаунте, где определены публичные модели и как они используются, чтобы лучше понимать кросс-проектные ресурсы.
- Используйте функциональность [Lenses](/docs/explore/explore-projects#lenses) — это слои, похожие на карты, для вашего DAG, доступные из графа lineage проекта. Lenses помогают глубже понять контекстные метаданные проекта в масштабе, особенно когда нужно выделить конкретную модель или подмножество моделей.
- Получайте доступ к column-level lineage (CLL) для ресурсов в вашем dbt-проекте. <Lifecycle status="managed,managed_plus" />

### Ограничения {#caveats-4}
- Для того чтобы <Constant name="explorer" /> начал заполнять информацию, в production-окружении деплоя должен быть выполнен как минимум один успешный job run.

Ознакомьтесь с возможностями <Constant name="explorer" />, чтобы максимально эффективно использовать его функциональность и избежать упущенных возможностей для повышения эффективности.

Дополнительную информацию смотрите в разделе [<Constant name="explorer" /> FAQs](/docs/explore/dbt-explorer-faqs).

## Что дальше? {#whats-next}

<ConfettiTrigger>

Поздравляем с завершением руководства 🎉!

Мы надеемся, что вы получили полезные рекомендации и практические советы, которые помогут вам в процессе перехода. Важно отметить, что переход с <Constant name="core" /> на <Constant name="cloud" /> — это не просто развитие ваших data‑проектов, а возможность выйти на новый уровень совместной работы, управления, эффективности и инноваций внутри команды.

В качестве следующих шагов рекомендуем продолжить изучение нашей серии из трёх руководств, посвящённых переходу с <Constant name="core" /> на <Constant name="cloud" />:

<CoretoCloudTable/>

</ConfettiTrigger>

### Ресурсы {#resources}

Если вам нужна дополнительная помощь или у вас есть вопросы, используйте следующие ресурсы:

- Курсы [dbt Learn](https://learn.getdbt.com) для обучения по запросу в формате видео.
- Наша [служба поддержки](/docs/dbt-support) всегда готова помочь вам с устранением проблем в <Constant name="cloud" />.
- Присоединяйтесь к [сообществу dbt](https://community.getdbt.com/), чтобы общаться с другими пользователями dbt, задавать вопросы и делиться лучшими практиками.
- Подпишитесь на [RSS-уведомления <Constant name="cloud" />](https://status.getdbt.com/)
- Для корпоративных аккаунтов доступна команда по управлению аккаунтами, которая помогает с решением технических вопросов и сопровождением аккаунта. [Запросите демо](https://www.getdbt.com/contact), чтобы узнать больше.
- [Сравнение <Constant name="cloud" /> и <Constant name="core" />](https://www.getdbt.com/product/dbt-core-vs-dbt-cloud) с подробным разбором различий между <Constant name="core" /> и <Constant name="cloud" />.

Для индивидуальной помощи вы можете использовать следующие ресурсы:

- Запишитесь на [демонстрации с экспертами](https://www.getdbt.com/resources/dbt-cloud-demos-with-experts) и получите инсайты
- Работайте с командой [Профессиональных услуг dbt Labs](https://www.getdbt.com/dbt-labs/services), чтобы поддержать вашу организацию данных и переход.
